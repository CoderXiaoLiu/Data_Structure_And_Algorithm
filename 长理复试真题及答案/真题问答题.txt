	【2012年】
1、综述C语言的数据类型
长理答案：1）基本类型：
整型：短整型short，基本整型int，长整型long，无符号整型unsigned
实（浮点）型：单精度float，双精度double，长双精度实型long double
字符类型：char
枚举类型：enum
2）构造类型：
数组，结构体struct，共用体union    
3）指针类型
4）空类型void

2、综述C语言的控制语句
长理答案：
顺序结构，从上往下依次执行
选择结构，对给定条件进行判断，根据判断结构执行语句
	主要的选择结构有 
	if（条件）{} 结构，条件如果成立则执行 {} 中的内容
	if（）{}else（）结构，条件成立执行第一个 {} 中的内容，否则执行 else{} 中的内容
	if（）{}else if（）{}else 结构，多了一个 else if 来增加另外控制流程
	switch 结构，通过 switch 中的值，和case 的值进行比较，匹配的则执行对应case下的语			句，如果没有break，则会继续往下一个case执行
循环结构，while（条件）{} 循环，当条件成立，则执行 {} 中的内容。
	do_while 循环先执行 do 中的语句，再通过 while 后的条件判断，如果条件成立，则继续		执行 do 中的语句
	for（语句1；条件；语句2）{ } 循环，当条件成立，则执行 {} 中的语句，语句1主要用来		做变量的初始化，语句2主要用来写每次循环都需要执行的语句。

	【2013年】
五、问答题（3*10=30）
1、简述C语言的特点及设计一个C语言程序流程
①移植性好：适用于不同平台，代码移植时只需做较少修改或不做修改
②能直接访问硬件
③运行效率高：C可以直接在机器上运行，一些高级语言需要相应的语言解释器做解释后，才能在机器上运行。
长理答案：C语言是中级语言，语言简洁，灵活，数据类型丰富，具有现代语言的各种数据结构，C语言中包含指针概念，能够直接操作内存程序可移植性好、能够使用多种操作系统。设计一个C语言程序的步骤：
长理答案二：
*语言简洁、紧凑，使用方便、灵活
*运算符丰富
*数据结构丰富，具有现代化语言的各种数据结构
*具有结构化的控制语句
*语法限制不太严格，程序设计自由度大
*C语言能进行位操作，能实现汇编语言的大部分功能，可直接对硬件进行操作
*生成目标代码质量高，程序执行效率高
*程序可移植性好（跟汇编语言比）
1）分析问题
2）画出程序的基本轮廓
3）编写程序
4）测试程序

2、叙述C语言循环控制结构类型及编程特点
长理答案：
do_while 循环，先执行一次，再通过循环条件循环
while 循环，先判断循环条件是否成立，成立则循环
for 循环，for（表达式1；表达式2；表达式3），表达式1主要用来初始化变量的值，表达式2是循环条件，表达式3是每循环一次的规律操作。

3、设计一个类，计算长方形的面积和周长
class Rectangle{
private:
	int length;
	int width;
public:
	Rectangle(int a,int b){
		this->a=a; this->b=b;}
	int area(){
		return (width*length);
	}
	int cir(){
		return (width+length)*2
	}
};
	
	【2016年】
五、问答题（2*5=10）
1、全局变量和局部变量有什么区别？是怎么实现的？
答：
1、全局变量的作用在这个程序块,而局部变量作用于当前函数
2、分配位置不同：全局变量分配在全局数据区,局部变量分配在栈区
3、生命周期不同：全局变量随主程序创建和创建，随主程序销毁而销毁，局部变量在局部函数内部，甚至局部循环体等内部存在，退出就不存在
4、使用方式不同：通过声明后全局变量程序的各个部分都可以用到，局部变量只能在局部使用
实现方式：
操作系统和编译器通过内存分配的位置来知道的全局变量分配在全局数据段，并且在程序被运行的时候就被加载。
编译器通过语法词法的分析，判断出是全局变量还是局部变量。如果是全局变量的话，编译器在将源代码翻译成二进制代码时就为全局变量分配好一个虚拟地址 (windows下0x00400000以上的地址，也就是所说的全局区)，所以程序在对全局变量的操作时是对一个硬编码的地址操做。
局部变量的话，编译时不分配空间，而是以相对于ebp或esp的偏移来表示局部变量的地址，所以局部变量内存是在局部变量所在的函数被调用时才真正分配。 以汇编的角度来看：函数执行时，局部变量在栈中分配，函数调用完毕释放局部变量对应的内存，另外局部变量可以直接分配在寄存器中。
操作系统通过变量的分配地址就可以判断出是局部变量和全局变量。 

2、C++中的什么是多态性？是如何实现的？
答：同样的调用语句有不同的表现形态
通过继承、并让子类重写父类的虚函数，并让父类指针指向子类对象。
答：多态性是指：向不同的对象发送同一个消息，不同的对象在接收时会产生不同的行为（即方法）
多态性是通过指针和虚函数来实现的

	【2017年】
五、问答题（2*5=10分）
1、(void *)ptr 和 (*(void**))ptr 的结果是否相同？说明理由。
答：相同。 (*(void**))ptr里面定义的是一个二级指针，对一个二级指针进行间接访问，得到的是一个一级指针，与第一个一样了。

2、试阐述堆和栈的区别？
长理答案：
1）栈，先进后出，堆的存取是任意的；
2）栈内存储的局部变量，堆存储的是实体；
3）栈内存的更新速度要快于堆内存，因为它是局部变量；
4）栈内存放的变量生命周期一旦传来就会被释放，堆的实体会被垃圾回收机制不定时回收。
长理答案二：
栈：由编译器自动分配并且释放，一般存储函数的参数局部变量等
堆：由程序员分配释放，若程序不释放则系统释放
区别一：申请内存方式
栈：由系统自动分配，如变量的声明的同时会开辟空间（静态指定）
堆：由程序员申请，需要制定大小（动态分配）
区别二：系统响应的不同
栈：只要剩余空间大于申请内存，系统就会提供，否则会栈溢出
堆：遍历空闲地址链表，找到符合要求的，就删除该地址分配给程序，内存的首地址记录分配的大小，（方便delete）多余的内存回收
区别三：空间大小不同‘
栈：连续的，编译时就确定的常数
堆：不连续，他的上限决定于系统中有效的虚拟内存
区别四：执行效率的不同
栈：由系统分配，速度快
堆：程序员分配，速度慢，容易产生内存碎片，不过用起来方便
区别五：执行函数的不同

	【2018年】
五、问答题（2*10=20）
1、C语言中，文件的基本操作过程是什么？常用的文件操作函数有哪些？
长理答案：先使用 fopen() 打开文件，获取文件指针，然后读写文件，最后通过 fclose() 关闭文件
C语言标准库中文件操作的函数主要有 fopen、fclose、frend、fwrite、fgetc、fputc、fgets、fputs、fprintf、fscanf等。

2、什么是面向对象程序设计？面向对象程序设计的基本特征是什么？
长理答案：它将某些具有共同属性的事务进行抽象成类，而对象则时类的实现，它将对象作为程序的基本单元，将属性和函数封装在其中，以提高软件的可重用性，灵活性，扩展性。
基本特征：
封装：封装性就是把对象的属性和方法结合成一个独立的单位，并尽可能隐蔽对象的内部细节
继承：继承的本质是在已有的类的基础上进行扩充或改造，得到新的数据类型，以满足新的需要
多态：多态性一般是指在父类定义的方法被子类继承后，可以表现出不同的行为，这使得同一个方法在父类及其子类中具有不同的语义。

	【2019年】
五、问答题（2*5=10）
1、如何在内存中存储数量不定的数据（如学生的成绩，学生可能100人，也可能有几万人）。
答：使用指针和动态分配内存，C++提供了一种“动态分配内存”的机制，使得程序可以在运行期间，根据实际需要，要求操作系统临时分配一片内存空间用于存放数据。这种内存分配是在程序运行中进行的，而不是在编译时就确定的，因此称为“动态内存分配”。在C++中通过new运算符来实现动态内存分配。程序从操作系统动态分配所得的内存空间在使用完后严格释放，交还操作系统，以便操作系统将这片内存空间分配给其它程序使用。C++提供delete运算符，用于释放动态分配的内存空间。

2、面向对象编程有哪些常用的概念？
答：多态，封装，继承，类，对象